# 问卷数据分析方案 — 区域健康风险预测平台

## 配套文件
- `问卷_项目反馈调查.html` — 可直接使用的移动端友好问卷
- `survey_analysis.py` — Python 数据分析脚本
- `survey_responses.json` — 收集到的回复数据

## 分析流程

### Phase 1: 数据清洗

1. **去重**: 相同浏览器指纹 + 提交间隔 < 2分钟 → 标记为重复
2. **缺失值**: 必填字段缺失 > 30% → 剔除该条记录
3. **逻辑校验**: 学生身份答教师专属题 → 标记可疑
4. **编码**: "N/A" → NaN, Likert 1-5 → int, identity → one-hot

### Phase 2: 描述统计

| 分析 | 方法 | 可视化 |
|------|------|------|
| 身份分布 | frequency count | 堆叠柱状图 |
| 学科背景 | proportion | 饼图/环形图 |
| 使用频率 | histogram | 柱状图(按身份分色) |
| 发现渠道 | cross-tab × identity | 堆叠条形图 |

### Phase 3: 满意度分析

| 指标 | 计算 |
|------|------|
| 各模块均值 | mean(score), 排除 N/A |
| 各模块标准差 | std(score) — 高方差 = 体验不一致 |
| NPS得分 | Promoters(9-10)% - Detractors(1-6)% |
| 模块排名 | 按均值降序排列 |

**热力图**: 模块 × 身份组 的均值矩阵

### Phase 4: 组间差异分析

| 比较 | 检验 |
|------|------|
| 学生 vs 教师 总体满意度 | Mann-Whitney U (非正态) |
| 不同经验水平 × 各模块 | Kruskal-Wallis + Dunn post-hoc |
| 不同学科 × 功能优先级 | Chi-squared test on feature selections |

### Phase 5: 文本分析

1. **分词**: jieba 中文分词
2. **关键词**: TF-IDF 提取高频词
3. **情感**: SnowNLP 情感极性分析
4. **聚类**: LDA 主题模型 (建议/痛点各3-5个主题)

### Phase 6: 优先级矩阵

**横轴**: 功能需求频次  
**纵轴**: 需求者平均满意度  
**四象限**:
- 🔴 **高需求 + 低满意度** → 紧急改进
- 🟡 **高需求 + 高满意度** → 扩大推广
- 🟢 **低需求 + 高满意度** → 维持
- ⚪ **低需求 + 低满意度** → 观察

## Python 分析脚本

```python
# -*- coding: utf-8 -*-
# survey_analysis.py — 问卷数据分析
import json, pandas as pd, numpy as np
import matplotlib.pyplot as plt
from scipy import stats

# 1. Load
with open('survey_responses.json','r',encoding='utf-8') as f:
    data = pd.DataFrame(json.load(f))

# 2. Clean
data = data.drop_duplicates(subset=['identity','field','timestamp'])
matrix_cols = [c for c in data.columns if c.startswith(('m_','ux_'))]
for c in matrix_cols: data[c] = pd.to_numeric(data[c].replace('na',np.nan), errors='coerce')

# 3. Descriptive
print("=== Demographics ===")
print(data['identity'].value_counts())
print(data['field'].value_counts())

# 4. Satisfaction by module
scores = data[matrix_cols].mean().sort_values(ascending=False)
print("\n=== Module Satisfaction Scores ===")
print(scores.round(2))

# 5. Overall NPS
data['nps_cat'] = pd.cut(data['recommend'].astype(float), bins=[0,6,8,10], labels=['Detractor','Passive','Promoter'])
print("\n=== NPS ===")
print(data['nps_cat'].value_counts())

# 6. Student vs Teacher
students = data[data['identity']=='student']
teachers = data[data['identity']=='teacher']
for c in matrix_cols:
    if c in students.columns and c in teachers.columns:
        try:
            stat, p = stats.mannwhitneyu(
                students[c].dropna(), teachers[c].dropna(), alternative='two-sided')
            if p < 0.05:
                print(f"  SIG: {c}: student={students[c].mean():.2f}, teacher={teachers[c].mean():.2f}, p={p:.3f}")
        except: pass

print("\nAnalysis complete.")
```

## 报告输出

生成 `analysis_report.html` 包含:
1. 执行摘要 (Key Findings)
2. 人口统计图表
3. 满意度热力图
4. NPS 仪表盘
5. 文本分析词云
6. 优先级矩阵
7. 改进建议 Top-10
